apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: engine
  namespace: argocd
spec:
  project: default
  sources:
    - repoURL: https://github.com/flair-hr/agentops-engine.git
      targetRevision: 2107ae4380a884be369f127a2090eb7b56eb1085
      path: charts/engine
      helm:
        releaseName: engine
        valueFiles:
          - $values/clusters/ops/engine/values.yaml
    - repoURL: https://github.com/flair-hr/agentops-platform.git
      targetRevision: main
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: dev-agents
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
