# SOPS configuration — age recipients and enforced encryption paths.
# Replace the placeholder recipient with the real platform age public key during M2 (docs/BOOTSTRAP.md step 2).
# RULE: everything under secrets/ MUST be encrypted; CI should reject plaintext there.
creation_rules:
  - path_regex: ^secrets/.*\.(yaml|yml|json|env)$
    age: "age1x5del8md896v3a53z89hss5ktjd0qq4cqlazjd6dkvewjxwy4ufse75yjy"
