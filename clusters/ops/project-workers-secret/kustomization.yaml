apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Applied into the argocd namespace, where the ApplicationSet controller reads
# the plugin-generator token (plugin-configmap.yaml: token: $argocd-plugin-token:token).
namespace: argocd

generators:
  - secret-generator.yaml
