apiVersion: viaduct.ai/v1
kind: ksops
metadata:
  name: project-workers-secret-generator
files:
  # Bearer token the ArgoCD project-workers plugin generator sends to the
  # gateway getparams endpoint. SAME value as the gateway's
  # secrets/engine/argocd-plugin-token.enc.yaml (they are written together).
  - ../../../secrets/argocd/argocd-plugin-token.enc.yaml
